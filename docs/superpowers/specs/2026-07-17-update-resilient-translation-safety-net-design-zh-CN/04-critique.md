# 译文审校记录

## 准确性

- 原文的限制、状态机、性能预算、测试门禁和开放问题均已完整保留。
- `translationId`、`fallbackMode`、状态枚举、代码、命令和路径均保持原值。
- 未将“全面兜底”误译为“保证任意新文案自动翻译”；原文关于未知文案必须隔离和评审的边界已保留。

## 术语一致性

- `surface` 统一译为“界面域”，避免与视觉表面混淆。
- `fallback` 统一译为“兜底”，`admission` 统一译为“准入”。
- `resilient hook` 初稿中出现了“弹性 hook”，应改为仓库既有说法“容错 hook”。
- `schema validation` 初稿中的“模式验证”容易与运行模式混淆，应改为“结构校验”。

## 表达调整

- 将“官方目录归属”调整为“官方语言目录归属”，明确这里的 catalog 指语言资源目录。
- 其余句式已经符合中文工程规格文档习惯，无需改变原文结构。

