---
description: 改代码必须先 TDD，无自我豁免
alwaysApply: true
---

# TDD 强制

1. 动生产代码前：Read `using-superpowers`，再 Read `test-driven-development`。
2. **RED**：先写/改测试，运行并确认**按预期失败**；未见失败不得写实现。
3. **GREEN**：最小实现 → `npm test` 全绿 → 再 REFACTOR。
4. 声称完成前：跑 `npm test`，回复里贴通过输出。
5. 禁止以优化、refactor、工具层、改动小、赶时间为由跳过 RED。
6. 已先写实现：删掉实现，从失败测试重来；禁止事后补测糊弄。
7. 禁止无失败测试就对生产代码做 extract/split 批量改写。
